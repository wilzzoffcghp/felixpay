export const metadata = {
  title: 'Digital Pedia H2H',
  description: 'Payment Gateway API'
};

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  );
}
